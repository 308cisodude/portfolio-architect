# Portfolio Architect Gateway — Comdirect v1.26.0

Version 1.26.0 keeps the established Comdirect App behavior unchanged while aligning package versions with Portfolio Architect. The separate Trade Republic App gains statement import; no PDF parser or Trade Republic code is added here.

The App uses its own `/data/gateway` private volume and must be upgraded in place to retain private state.
