# Portfolio Architect Gateway — Trade Republic v1.43.0

Version 1.43.0 is package alignment for the Portfolio Architect route-level evidence and native funding-edge editing release. The separate strict local `DEPOTAUSZUG` holdings and `KONTOAUSZUG` cash-statement imports introduced in v1.41.0 are unchanged.

Verified HTTPS/private CA trust, bearer
authentication, REST schema 1, health schema 6 and accepted snapshot serving are unchanged.

Upgrade in place to retain App-private TLS state and the accepted provider snapshot. No
statement re-import is required merely because of the upgrade.
