# Supported versions

## Portfolio Architect

The current stable release and the immediately preceding stable release receive
security and correctness fixes while a documented upgrade path exists. Older
releases may continue to work but are not actively supported.

## Home Assistant

The 1.36.x release line requires Home Assistant 2026.7.0 or newer. CI validates
project tests on Python 3.14, HACS metadata, and the deliberately pinned hassfest
validator snapshot. The project targets the current and immediately previous
monthly Home Assistant release.

Scheduled HACS and hassfest workflows re-run the reviewed immutable validator
images. They do not silently adopt upstream image changes. Maintainers must review
and update those digests deliberately when validating against a newer upstream
snapshot.

## Gateway compatibility

Portfolio Architect 1.56.1 accepts REST portfolio schema 1, including the optional
additive `investment_cash` authorization metadata, and Gateway health schemas 1
through 8. Health schema 9 adds bounded acquisition-method inventory, readiness,
explicit no-fallback policy and operator switch history; schema 7 continues to carry
bounded `acquisition_mode`, schema 6 carries bounded provider identity, and schemas
1 through 7 remain accepted unchanged. Gateway App 1.16.1 and later remain compatible with the
legacy reserve contract; Gateway App 1.19.0 or newer is required to configure cash
authorization policies, and 1.19.1 or newer contains the corrected
capped-to-all-available Ingress transition. Gateway App 1.56.1 is version-aligned with the current stable integration.
Canonical `portfolio_architect_gateway_comdirect` remains stable. Historical `portfolio_architect_gateway` was deprecated through its final v1.56.x migration-source line and is withdrawn from the active repository in v1.58.0; canonical Comdirect retains bounded migration-receiver compatibility for already-installed supported Legacy instances. DKB and Trade Republic are stable for their supported local acquisition paths; the DKB anonymous FinTS BPD probe remains experimental/research-only and authenticated FinTS remains disabled. Generic Import remains experimental despite its successful isolated v1.52.0 live smoke. DKB supports bounded local depot-CSV acquisition through its canonical verified-HTTPS Gateway snapshot; v1.46.0 removes the completed PA-side legacy DKB CSV bridge and the no-longer-needed migration-only endpoint; v1.47.0 adds independent DKB Girokonto CSV cash evidence using the existing REST-schema-1 optional investment-cash fields and imported-statement freshness; v1.48.0 moves complete Comdirect static CSV acquisition into the Comdirect Gateway with explicit `live_api`/`csv` arbitration, no silent fallback, and health-schema-7 acquisition mode; v1.48.1 uses that mode for acquisition-aware freshness and cadence-aware static defaults while preserving explicit thresholds; v1.48.2 fixes live coordinator source-summary propagation so the mode reaches freshness evaluation after normal and unchanged-snapshot refreshes; v1.49.0 retires the completed PA-side Comdirect CSV migration parser/bridge while preserving Gateway acquisition and freshness semantics; v1.50.0 makes the single-primary/supplemental source model explicit in Configure and adds DKB probe-dispatch timestamp observability without changing provider acquisition; v1.51.0 moves the remaining provider-neutral mapped CSV acquisition into a dedicated Generic Import Gateway and removes the DKB UI hard-coded timezone while preserving canonical UTC; v1.51.1 fixes only the stale Home Assistant coordinator local-source attribute reference exposed by that cleanup; v1.52.0 graduates the live-proven DKB CSV and Trade Republic PDF Apps to stable while keeping the DKB FinTS probe capability experimental and Generic Import experimental; v1.53.0 adds health-schema-8 acquisition-control metadata and explicit atomic Comdirect `live_api`/complete-`csv` switching with no automatic fallback while Portfolio Architect remains read-only; v1.53.1 fixes method-switch timestamp acceptance, static snapshot serving and unavailability classification without changing that control plane; v1.54.0 standardizes Gateway acquisition-state colours, removes static-Gateway freshness-looking options and adopts branch-current Alpine OpenSSL installation with a protected minimum security floor; v1.55.0 adds the staged Comdirect App-identity migration; v1.55.1 accepts production acquisition-state schema 2 and adds bounded migration preflight/diagnostics; v1.56.0 adds only UX/hygiene corrections and keeps the same acquisition/wire/security contracts; v1.56.1 fixes only the post-cut-over Comdirect OAuth-session restart classification while retaining strict pre-cut-over OAuth exclusion and the same migration trust invariants; v1.58.0 withdraws only the historical Comdirect App package/publication surface while retaining the canonical migration receiver; the v1.28 anonymous FinTS BPD probe remains a separate research gate and authenticated FinTS remains disabled. Trade Republic retains the v1.25 documented local `DEPOTAUSZUG` statement-import family and serves accepted holdings through REST schema 1; in v1.26 its App auto-starts and Portfolio Architect can consume it as an additional authenticated Gateway alongside the existing primary REST source. Version 1.26.1 corrects target matching for ISIN-only provider snapshots without changing either wire schema. Version 1.26.2 adds Home Assistant presentation/diagnostic metadata. Version 1.26.3 is a dashboard/presentation follow-up and keeps both wire schemas unchanged. Version 1.26.4 attempted native date-tile formatting; Version 1.26.5 adds only read-only Home Assistant `date.*` presentation counterparts and likewise keeps both wire schemas unchanged. Version 1.26.6 fixes non-live REST Gateway source identification. Version 1.26.7 fixes cached-snapshot quantity persistence and HTTP validator precedence. Version 1.27.1 is the first successfully published form of the verified private-PKI HTTPS transport; live acceptance then exposed that the manifest-level `single_config_entry` gate prevented Supervisor discovery from reaching an already-configured entry. Version 1.27.2 fixes that Home Assistant config-flow eligibility issue while retaining the same TLS/runtime contracts. Version 1.27.3 fixes the residual DKB Gateway `dkb` versus DKB CSV `dkb_csv` discovery-suppression mismatch without changing TLS/runtime contracts. Version 1.27.4 keeps those contracts and moves short-lived Comdirect OAuth renewal onto a provider-specific maintenance cadence independent of portfolio polling. Version 1.28.0 keeps the same wire/TLS contracts and adds only the DKB anonymous FinTS capability-probe milestone; DKB live holdings remain disabled pending product registration and authenticated user-capability validation. Version 1.28.1 refreshes the immutable GitHub Actions runtime pins only; provider runtime, wire/TLS contracts and the DKB gate remain unchanged. Version 1.28.2 groups GitHub Actions Dependabot version updates only; provider runtime, wire/TLS contracts and the DKB gate remain unchanged. Version 1.29.0 adds only native policy-dashboard presentation hierarchy and aligned package metadata. Version 1.30.0 adds provider-aware local execution-policy planning while Gateway runtime, wire/TLS contracts and the DKB gate remain unchanged. Version 1.31.0 corrects the canonical Robotics share class and historical exception lifecycle; v1.31.1 restores the established ISIN-first contract for outside-scope holdings whose provider supplies no WKN. Version 1.31.2 hardens the experimental DKB FinTS product-registration/probe diagnostics and Ingress navigation while keeping live DKB acquisition disabled and the same REST/health/TLS contracts. Version 1.32.0 adds per-source freshness observability and cross-provider diagnostic hardening while preserving aggregate freshness/actionability semantics. Version 1.33.0 separates source-evidence freshness from recurring plan scheduling and adds explicit conservative evidence-kind freshness policy; v1.33.1 corrects the remaining oldest-source recurring-schedule anchor while keeping the same REST/health/TLS contracts. Version 1.34.0 adds opaque target identity and a structural presentation model in the Home Assistant/engine layer while retaining those REST/health/TLS contracts and schema-1 plan compatibility; v1.34.1 fixes whole-portfolio allocation presentation and ISIN-first reference-dashboard bindings without changing those contracts. Version 1.35.0 adds provider-scoped authorized cash and explicit directed advisory funding topology in the Home Assistant/engine layer, while REST portfolio schema 1, Gateway health schema 6, provider isolation and verified private-PKI transport remain unchanged. Version 1.35.1 fixes the Comdirect OAuth-maintenance resilience edge case where a direct connection reset could escape transport classification and terminate the maintenance worker. Version 1.35.2 adds native provider/funding configuration, explicit non-economic promotional metadata semantics, and retained-cash authorization while preserving REST schema 1 and health schema 6. Version 1.35.3 fixes only the missing labels in the native broker-editor menus. Version 1.35.4 fixes locale-tolerant cash-policy amount entry and bounded invalid-amount Ingress feedback. Version 1.36.0 adds only Home Assistant-side native dynamic presentation slots and reference-dashboard consumption; Version 1.36.1 fixes the live-observed dynamic allocation-card composition and compact entity-only naming. Version 1.37.0 adds shared opt-in human-numeric Gateway validation and migrates the established Comdirect cash fields onto the shared EUR primitive; Version 1.38.0 adds Home Assistant-side copy-friendly recommendation ISIN interaction and policy-aware cash context; Version 1.38.1 restores native dynamic signed per-target drift bars through the existing bounded presentation slots; Version 1.39.0 adds paired colourful current/target allocation Tiles through those same bounded presentation slots; Version 1.40.0 adds optional source/date provenance and freshness gating to exact advisory funding-transfer edges while preserving legacy schema-3 compatibility; Version 1.40.1 fixes native Configure-form selector/date compatibility and bounded duplicate feedback without changing those funding or Gateway contracts; Version 1.41.0 adds a separate local Trade Republic `KONTOAUSZUG` cash-statement family, keeps cash/holdings evidence timestamps independent and uses the existing schema-1 additive cash fields; Gateway wire/security contracts remain unchanged. Version 1.41.1 changes only the Home Assistant-side funded-route tie-break so sufficient local execution-provider cash wins an otherwise identical transfer-funded candidate; Gateway wire/security and statement-import contracts remain unchanged. Version 1.42.0 adds a Home Assistant-side normalized execution-path sensor and native dashboard renderer for the already-decided plan; Gateway wire/security, provider acquisition, and routing semantics remain unchanged. Version 1.43.0 adds independent per-savings-plan-route evidence provenance/freshness and native editing of exact directed funding edges while preserving broker schemas, route economics, provider acquisition, and the v1.42 presentation contract. Version 1.44.0 adds only Home Assistant Configure menu/edit-context consistency and bilingual regression coverage; all planner, provider, schema and presentation contracts remain unchanged. Version 1.45.0 moves active DKB depot-CSV acquisition into the DKB Gateway and adds an exact atomic migration from legacy `dkb_csv` paths while keeping FinTS authentication disabled and all wire schemas unchanged. Version 1.45.1 fixes the stale-legacy comparison edge case and expired-health consistency without changing normal snapshot freshness or any published wire schema. Legacy HTTP remains supported only as a bounded in-place migration state when upgrading from pre-v1.27 installations.

## Security fixes

A release that repairs a security vulnerability will document the affected
versions and remediation path in the corresponding security advisory. Users
should not rely on an unsupported release merely because it still starts.
