# Portfolio Architect v1.64.4 upgrade and live acceptance

Update the integration and version-aligned Gateway Apps. Confirm integration/engine and App versions show 1.64.4. A Home Assistant restart is unnecessary if the integration has reloaded and both version diagnostics have advanced. No dashboard import or configuration migration is needed.

Verify DKB CSV holdings/cash authority, then make one read-only holdings research request through the DKB Gateway admin Ingress. Approve DKB app login and any separate holdings challenge, using the Gateway's check button after each decoupled approval. On `retrieved`, inspect the two independent tables within five minutes. Review ISIN, quantity, value and source timing yourself; a difference does not automatically indicate an error. The FinTS total-value currency is unavailable in the pinned parser. Do not copy sensitive position detail into issue reports; share only bounded outcomes and general observations.

The transient view disappears after five minutes or App restart. DKB CSV remains authoritative regardless of the displayed data. A successful visual review does not authorize a FinTS acquisition switch.
